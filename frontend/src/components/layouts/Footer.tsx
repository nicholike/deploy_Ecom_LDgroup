'use client';

import React from 'react';
import { Footerdemo } from '@/components/ui/footer-section';

/**
 * Footer Component - Tailwind Version
 * Replaces the old Bootstrap template footer
 */
export const Footer: React.FC = () => {
  return <Footerdemo />;
};
