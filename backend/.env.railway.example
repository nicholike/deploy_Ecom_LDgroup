# ============================================
# RAILWAY ENVIRONMENT VARIABLES
# Copy these to Railway dashboard
# ============================================

# ============================================
# 1. DATABASE (Railway tự tạo khi add MySQL)
# ============================================
DATABASE_URL="mysql://user:password@host:port/database"

# ============================================
# 2. JWT SECURITY (BẮT BUỘC - Tạo random string 32+ chars)
# ============================================
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_REFRESH_SECRET="your-super-secret-refresh-jwt-key-change-this-in-production"

# ============================================
# 3. EMAIL SMTP (Để gửi email reset password, order confirmation)
# ============================================
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASSWORD="your-gmail-app-password"

# ============================================
# 4. FRONTEND URL (Để tạo link reset password)
# ============================================
FRONTEND_URL="https://your-app.vercel.app"

# ============================================
# 5. BANK ACCOUNT INFO (Hiển thị cho user chuyển khoản)
# ============================================
BANK_ACCOUNT_NUMBER="6201235752"
BANK_ACCOUNT_NAME="DIEP DUC LAI"
BANK_CODE="BIDV"
BANK_NAME="BIDV"

# ============================================
# 6. SEPAY WEBHOOK (Payment gateway)
# ============================================
SEPAY_API_KEY="your-sepay-api-key"

# ============================================
# GHI CHÚ:
# ============================================
# ❌ KHÔNG CẦN SET các biến sau (đã có default trong code):
# - NODE_ENV (Railway tự set = production)
# - PORT (Railway tự set)
# - API_PREFIX (default = /api/v1)
# - JWT_EXPIRES_IN, JWT_REFRESH_EXPIRES_IN (default = 7d)
# - COMPANY_NAME, COMPANY_WEBSITE, COMPANY_EMAIL, COMPANY_HOTLINE (có default)
# - SMTP_FROM (có default)
# - Commission rates (hard-code trong commission.constant.ts)
# - CORS_ORIGIN (không cần, dùng FRONTEND_URL)
